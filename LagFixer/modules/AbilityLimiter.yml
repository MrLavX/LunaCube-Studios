AbilityLimiter:
  enabled: true
  worlds: # Use '*' to enable for all worlds.
    - '*'
  values:
    elytra_boost:
      cooldown: 3
      # Option disabled by developer
      speed_multiplier: 0.85
      # Set 0 to disable
      additional_durability_loss: 4

    trident_riptide:
      cooldown: 2
      # Option disabled by developer
      speed_multiplier: 0.85
      # Set 0 to disable
      additional_durability_loss: 4
